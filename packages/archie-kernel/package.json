{
  "name": "@beamlab/archie-kernel",
  "version": "1.0.0",
  "private": true,
  "description": "The Runtime OS and central coordinator for Archie",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/ --ext .ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@beamlab/context-engine": "workspace:*",
    "@beamlab/tool-registry": "workspace:*",
    "@beamlab/utils": "workspace:*",
    "@beamlab/planning-engine": "workspace:*",
    "@beamlab/execution-graph": "workspace:*",
    "@beamlab/task-scheduler": "workspace:*",
    "@beamlab/knowledge-platform": "workspace:*",
    "@beamlab/policy-engine": "workspace:*",
    "@beamlab/resource-manager": "workspace:*",
    "@beamlab/memory-system": "workspace:*",
    "@beamlab/solver-runtime": "workspace:*",
    "@beamlab/agent-framework": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "^6.0.3",
    "vitest": "^1.6.1",
    "rimraf": "^5.0.5"
  }
}
