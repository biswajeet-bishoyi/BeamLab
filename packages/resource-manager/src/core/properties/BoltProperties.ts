export interface BoltProperties {
  grade: string; // e.g. '8.8'
  diameter: number; // in mm
  holeDiameter: number;
  yieldStrength: number;
  ultimateStrength: number;
  shearStrength: number;
  tensileStressArea: number;
}
